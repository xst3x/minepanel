/**
 * folia.js — Folia (PaperMC fork with multithreading)
 * Uses the same PaperMC Fill v3 API as Paper/Velocity/Waterfall.
 */
'use strict';

const PaperResolver = require('./paper')
export = new PaperResolver('folia');
