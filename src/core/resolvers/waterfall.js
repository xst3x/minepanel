/**
 * waterfall.js — Waterfall (BungeeCord fork by PaperMC)
 * Uses the same PaperMC Fill v3 API as Paper / Velocity / Folia.
 */
'use strict';

const PaperResolver = require('./paper');

module.exports = new PaperResolver('waterfall');
