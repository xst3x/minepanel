/**
 * velocity.js — Velocity proxy (PaperMC)
 * Uses the same PaperMC Fill v3 API as Paper/Folia/Waterfall.
 */
'use strict';

const PaperResolver = require('./paper');

module.exports = new PaperResolver('velocity');
